spring:
  datasource:
    username: user
    password: password
    url: jdbc:postgresql://localhost:5432/book-store
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database: postgresql

system-configuration:
  public-endpoints: /api/auth/register, /swagger-ui/**
